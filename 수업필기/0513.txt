<리스트>
*b : 가변인수(하나 또는 여러개의 값을 가질 수 있는 변수)

append: 값을 붙여줌
extend: 리스트를 붙여줌

copy:값만 복사하기 때문에 id가 다르다.

-list와 dictionary(:key-value)를 가장 많이 사용
-string 과 tuple은 값을 수정할 수 없다.
-set: 집합, 중복데이터를 갖지않는다.

*내일, PPT에 나와있는 LIST시험지 그대로 시험*

<딕셔너리 test>
